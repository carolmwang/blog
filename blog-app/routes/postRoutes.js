const router = require('express')();
const postController = require('../controllers/postController');

// Get all posts /posts

// Get one post /posts/:id

// Update post /users/:id/posts/:id

// Create post /posts

// Delete post /posts/:id

module.exports = router;