const router = require('express')();
const commentController = require('../controllers/commentController');

// Get all comments /posts/:id/comments

// Get all user's comments /users/:id/comments

// Get one comment /posts/:id/comments/:id

// Get one user comment /users/:id/comments/:id

// Update one comment /users/:id/comments/:id

// Create comment /posts/:id/comments

// Delete comment /users/:id/comments/:id

module.exports = router;